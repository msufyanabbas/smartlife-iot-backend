export enum ShareResourceType {
  DASHBOARD = 'dashboard',
  DEVICE = 'device',
  ASSET = 'asset',
  REPORT = 'report',
  FLOOR_PLAN = 'floor_plan',
}

export enum ShareType {
  EMAIL = 'email',
  LINK = 'link',
}

export enum AccessLevel {
  VIEW = 'view',
  EDIT = 'edit',
  ADMIN = 'admin',
}