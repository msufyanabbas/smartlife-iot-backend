// src/common/enums/node.enum.ts 
export enum NodeType {
  FILTER = 'filter',
  ENRICHMENT = 'enrichment',
  TRANSFORMATION = 'transformation',
  ACTION = 'action',
  EXTERNAL = 'external',
  FLOW = 'flow',
  SCRIPT = 'script',
  SWITCH = 'switch',
}